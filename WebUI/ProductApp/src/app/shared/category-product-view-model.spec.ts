import { CategoryProductViewModel } from './category-product-view-model';

describe('CategoryProductViewModel', () => {
  it('should create an instance', () => {
    expect(new CategoryProductViewModel()).toBeTruthy();
  });
});
